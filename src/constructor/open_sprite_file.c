#include "constructor.h"

/* 
	Checks for file format in a file name 
*/

void	open_sprite_file(t_data *data)
{
	//similar stuff to cub file should happen here
	(void)data;
	// file_to_heap("./map_files/sprite_info.ini", &tmp);

}